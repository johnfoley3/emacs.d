;;; For settings and packages that I don't know where to put or don't want to make a whole file for.

(require 'delight)
(delight '(
           (yas/minor-mode)
           (abbrev-mode)
           ))
